#include "main.h"

/**
 * _isdigit - alphabet checker.
 * @c: integer
 *
 * Return: if c digit  print 1, else print 0.
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else

	return (0);
}
