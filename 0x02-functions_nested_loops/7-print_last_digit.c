#include "main.h"

/**
 * print_last_digit - print the last digit of n.
 *@n: integer.
 *
 * Return: result.
 */

int print_last_digit(int n)
{
	int 1 = n % 10;

	if (n < 0)
{
	1 = 1 * -1;
}
	_putchar('0' + 1);
	return (1);
}
