#include "main.h"

/**
 * _strlen - check the code.
 * @s: character.
 *
 * Return: length of a string.
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		i = i;
	}
	return (i);
}
